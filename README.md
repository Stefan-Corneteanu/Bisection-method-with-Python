Bisection method with Python

A short Python program to find an approximate root of the ecuation x^2-2=0 using the bisection method. The main focus of the code is working with the matplotlib.pyplot and the numpy libraries.